# Game related
Potentially make the processing order relative to order of play, rather than priority bracket/random order
# QoL
Maybe add a timer to the turns; after it ends players are required to pass (discard cards in 10 seconds if needed)